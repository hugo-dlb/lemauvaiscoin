{
  "name": "lemauvaiscoin",
  "version": "1.0.0",
  "scripts": {
    "start": "npm install --prefix lmc_back && npm install --prefix lmc_front && npm run build --prefix lmc_front",
    "postinstall": "npm start --prefix lmc_back"
  }
}